# src/ — Core Package

This is a proper Python package containing all reusable, testable, and production-ready code.

Import from anywhere:

```python
from src.data import DataLoader
from src.indicators import TechnicalIndicators
```
